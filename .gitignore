# Internal/admin pages — kept LOCAL only, never deployed to the public site.
# These expose infrastructure links (Cloudflare account, dashboards, etc.).
# Open them as local files on the Mac instead of via a public URL.
/quick-reference.html
/flowchart.html
/flowcharts/
/sitemap.html

# Stray local notes / context — NEVER publish (infra + project context, not site content)
/quick_reference_urls.md
/claude_project_context.md

# Wrangler CLI cache (account ID + page metadata; not secrets but no need to track)
.wrangler/

# Eleventy proof of concept (Phase 1) — dependencies and build output
node_modules/
_site/

# wrangler local secrets — never commit
.dev.vars
